import 'package:flutter/widgets.dart';

class FFButtonOptions extends StatelessWidget {
  Key key;
  int width;
  int height;
  Color color;
  TextStyle textStyle;
  int elevation;
  BorderSide borderSide;
  int borderRadius;


  FFButtonOptions({this.key, this.width, this.height, this.color, this.textStyle,
      this.elevation, this.borderSide, this.borderRadius});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
